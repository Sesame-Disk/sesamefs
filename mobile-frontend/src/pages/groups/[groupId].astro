---
import AuthenticatedLayout from '../../layouts/AuthenticatedLayout.astro';
import GroupDetail from '../../components/pages/GroupDetail';

export function getStaticPaths() {
  return [];
}

const { groupId } = Astro.params;
---

<AuthenticatedLayout title="Group Detail" pageTitle="Group Detail">
  <GroupDetail client:load groupId={groupId!} />
</AuthenticatedLayout>
